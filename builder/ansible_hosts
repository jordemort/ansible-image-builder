---
all:

  hosts:
    localhost:
      ansible_connection: local

    rootfs:
      ansible_connection: chroot
      ansible_host: /mnt/cache/rootfs

  vars:
    ansible_python_interpreter: /usr/bin/python3
