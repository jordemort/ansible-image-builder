---
debootstrap_mirror: "{{ lookup('env', 'DEBOOTSTRAP_MIRROR') | default('http://deb.debian.org/debian/', true) }}"
debootstrap_suite: "{{ lookup('env', 'DEBOOTSTRAP_SUITE') | default('buster', true) }}"
proxy_env:
